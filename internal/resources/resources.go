package resources

import (
	"github.com/mark3labs/mcp-go/server"
)

// Register registers all resources with the MCP server.
func Register(s *server.MCPServer) {
	// TODO: Register your resources here
	// Example:
	// s.AddResource(myResource, myResourceHandler)
}
