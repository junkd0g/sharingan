package prompts

import (
	"github.com/mark3labs/mcp-go/server"
)

// Register registers all prompts with the MCP server.
func Register(s *server.MCPServer) {
	// TODO: Register your prompts here
	// Example:
	// s.AddPrompt(myPrompt, myPromptHandler)
}
